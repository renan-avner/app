<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>   <!-- usar o conteudo da pagina _user nesse lugar especifico-->
  <% end %>
</ul>

<%= will_paginate %>